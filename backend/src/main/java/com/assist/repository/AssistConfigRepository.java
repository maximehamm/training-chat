package com.assist.repository;

import com.assist.model.entity.AssistConfig;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AssistConfigRepository extends JpaRepository<AssistConfig, Long> {
}
